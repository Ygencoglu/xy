<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            background-color: #000;
            color: #fff;
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 20px;
            background-color: #333;
        }

        .user-info {
            display: flex;
            align-items: center;
        }

        .user-info img {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            margin-right: 10px;
        }

        main {
            display: flex;
            justify-content: center;
            align-items: flex-start;
            padding: 20px;
        }

        .sidebar {
            flex: 1;
            margin-right: 20px;
        }

        .content {
            flex: 1;
        }

        .tweet-container {
            background-color: #333;
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 20px;
        }

        .tweet {
            display: flex;
            margin-bottom: 20px;
        }

        .tweet img {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            margin-right: 10px;
        }

        .tweet-content {
            flex: 1;
        }

        .tweet-actions {
            display: flex;
            align-items: center;
        }

        .tweet-actions i {
            margin-right: 10px;
            cursor: pointer;
        }

        .tweet-actions i:hover {
            color: #45a049;
        }

        .tweet-stats {
            color: #777;
            font-size: 0.8em;
        }

        .tweet-stats i {
            margin-right: 5px;
        }

        textarea {
            width: calc(100% - 20px);
            padding: 10px;
            margin-bottom: 10px;
            border: none;
            border-radius: 5px;
            background-color: #333;
            color: #fff;
        }

        button {
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            background-color: #4CAF50;
            color: white;
            cursor: pointer;
        }

        button:hover {
            background-color: #45a049;
        }

        .about {
            background-color: #333;
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 20px;
        }

        .about ul {
            padding-left: 20px;
        }

        .about li {
            margin-bottom: 5px;
        }

        footer {
            background-color: #333;
            padding: 20px;
            text-align: center;
        }

        .message-icon {
            font-size: 24px;
            color: white;
            text-decoration: none;
        }

        .message-icon:hover {
            color: #45a049;
        }

        .user-icon {
            font-size: 24px;
            color: white;
            text-decoration: none;
        }

        .user-icon:hover {
            color: #45a049;
        }

        .home-icon {
            font-size: 24px;
            color: white;
            text-decoration: none;
        }

        .home-icon:hover {
            color: #45a049;
        }

        .home-icon.active {
            color: #45a049;
        }

        .search-container {
            position: relative;
            display: flex;
            align-items: center;
            margin-bottom: 20px;
        }

        .search-container input[type="text"] {
            width: 200px;
            padding: 10px;
            border: none;
            border-radius: 5px;
            background-color: #333;
            color: #fff;
            margin-right: 10px;
        }

        .search-container button {
            background-color: transparent;
            border: none;
            cursor: pointer;
        }

        .editable-text {
            display: inline-block;
            margin-right: 10px;
            /* Sağa boşluk ekleyerek simge ile metin arasında bir mesafe oluşturur */
        }

        .edit-icon {
            cursor: pointer;
        }

        .about {
            background-color: #333;
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 20px;
        }

        .about h2 {
            color: #fff;
            margin-bottom: 10px;
        }

        .about form {
            display: flex;
            flex-direction: column;
        }

        .about input[type="text"],
        .about input[type="date"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 10px;
            border: none;
            border-radius: 5px;
            background-color: #444;
            color: #fff;
            outline: none;
        }

        .about input[type="date"]::-webkit-calendar-picker-indicator {
            filter: invert(1);
        }

        .about button {
            width: fit-content;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            background-color: #4CAF50;
            color: white;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .about button:hover {
            background-color: #45a049;
        }       
        .user-info-container {
            margin-top: 20px;
        }
        
        
    </style>
</head>

<body>
    <header>
        <div class="user-info">
            <img src="/images/xy.png" alt="User Avatar">
            <span>
                <%= username %>
            </span>
        </div>
        <a href="/home" class="home-icon"><i class="fas fa-home"></i></a>
        <a href="/messages" class="message-icon"><i class="fas fa-envelope"></i></a>
        <a href="/user" class="user-icon"><i class="fas fa-user"></i></a>
        <form action="/logout" method="POST">
            <button type="submit">Logout</button>
        </form>
    </header>
    <main>
        <div class="sidebar">
            <div class="search-container">
                <input type="text" placeholder="Ara...">
                <button type="button"><i class="fas fa-search"></i></button>
            </div>
            <div class="about">
                <h2>About</h2>
                <form action="/user" method="POST">
                    <input type="text" name="about" placeholder="Hakkımda">
                    <input type="text" name="city" placeholder="Şehir">
                    <input type="date" name="birthdate" placeholder="Doğum Tarihi">
                    <button type="submit">Kaydet</button>
                    <div class="user-info-container">
                        <div class="user-info-container">
                            <div class="user-info">
                                <p><strong>Kullanıcı Adı:</strong> <%= username %></p>
                                <p><strong>Hakkımda:</strong> <%= user.about %></p>
                                <p><strong>Şehir:</strong> <%= user.city %></p>
                                <p><strong>Doğum Tarihi:</strong> <%= user.birthdate %></p>
                            </div>
                        </div>                        
                    </div>                    
                </form>
            </div>
        </div>
        <div class="content">
            <!-- Tweetlerin listeleneceği bölüm -->
            <% tweets.forEach(tweet=> { %>
                <div class="tweet-container">
                    <div class="tweet">
                        <img src="/images/xy.png" alt="User Avatar">
                        <div class="tweet-content">
                            <% if (tweet.author) { %>
                                <span>
                                    <%= username %>
                                </span>
                                <% } %>
                                    <p>
                                        <%= tweet.content %>
                                    </p>
                        </div>
                        <div class="tweet-actions">
                            <i class="fas fa-heart"></i>
                            <i class="fas fa-comment"></i>
                            <i class="fas fa-share"></i>
                        </div>
                        <div class="tweet-stats">
                            <!-- İstatistikler buraya eklenebilir, şimdilik boş bırakıyorum -->
                        </div>
                    </div>
                </div>
                <% }) %>
                    <!-- Yeni tweet gönderme formu -->
                    <form action="/tweet" method="POST">
                        <textarea name="tweet" placeholder="Ne düşünüyorsunuz?"></textarea>
                        <button type="submit">Tweet</button>
                    </form>
        </div>
    </main>
</body>
<script>
    // JavaScript ile metin düzenleme işlevselliği
    function editText(fieldId) {
        const field = document.getElementById(fieldId);
        const newText = prompt('Yeni metni girin:', field.textContent);

        if (newText !== null) { // Kullanıcı iptal düğmesine tıklamazsa
            field.textContent = newText;
        }
    }
</script>

</html>